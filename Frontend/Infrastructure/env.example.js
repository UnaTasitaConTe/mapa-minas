/**
 * Configuración general para la aplicación.
 * Contiene las URL de conexión y el token de Mapbox.
 */
export const settings = {
  /**
   * La URL base para las conexiones API de la aplicación.
   * @type {string}
   * @example "http://localhost:3000/api"
   */
  urlConnection: "http://localhost:3000/api",
  /**
   * El token de acceso para la API de Mapbox.
   * Utilizado para la autenticación y carga de mapas.
   * @type {string}
   * @example "token_mapbox"
   */
  mapBoxToken: "token",
};
